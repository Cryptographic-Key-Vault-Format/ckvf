# TEST KEY — NEVER USE IN PRODUCTION

Private keys, MSK seeds, PKCS #8, and OpenPGP TSKs used by CKVF vectors are **test material**.

- Treat every byte here and in `../vectors/` as public and compromised.
- Do not import them into production agents, hardware tokens, or live identity services.
- Do not reuse these bytes as product documentation examples that look like real user keys.
